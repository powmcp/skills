interface:
  display_name: "EPUB retailer preflight"
  short_description: "Check exact EPUB before upload"
policy:
  products: ["CHAT", "CODEX"]
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "powmcp-epub-check"
      description: "Ebook File Check (PowMCP)"
      transport: "streamable_http"
      url: "https://powmcp.com/epub-check/mcp"
